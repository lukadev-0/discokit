{/* global props */}

import { Tabs, Tab } from "nextra-theme-docs";
import { Callout } from "nextra-theme-docs";
import { Pre } from "nextra/components/pre";

export function CodeBlock(props) {
  return (
    <Pre data-theme="default">
      <code
        className="nx-border-black nx-border-opacity-[0.04] nx-bg-opacity-[0.03] nx-bg-black nx-break-words nx-rounded-md nx-border nx-py-0.5 nx-px-[.25em] nx-text-[.9em] dark:nx-border-white/10 dark:nx-bg-white/10"
        data-language="js"
        {...props}
      />
    </Pre>
  );
}

<Tabs items={["Discokit", "Standalone"]}>
  <Tab>
    This package is available within the [`discokit`](/reference/discokit) package.

    <CodeBlock>
      <span className="line">
        <span className="text-[var(--shiki-token-keyword)]">import</span>
        <span className="text-[var(--shiki-color-text)]"> \{ ... } </span>
        <span className="text-[var(--shiki-token-keyword)]">from </span>
        <span className="text-[var(--shiki-token-string-expression)]">"discokit/{props.name}"</span>
      </span>
    </CodeBlock>

    <Callout>
      **For TypeScript Users**\
      The `"moduleResolution"` compiler option must be set to either
      `node16`, `nodenext` or `bundler` for the import to work.
    </Callout>

  </Tab>
  <Tab>
    This package can be installed as a standalone package:

    <Tabs items={["npm", "pnpm", "yarn"]}>
      <Tab>
        <CodeBlock>
            <span className="line">
              <span className="text-[var(--shiki-color-text)]">npm </span>
              <span className="text-[var(--shiki-token-string)]">i {props.packageName}</span>
            </span>
        </CodeBlock>
      </Tab>
      <Tab>
        <CodeBlock>
            <span className="line">
              <span className="text-[var(--shiki-color-text)]">pnpm </span>
              <span className="text-[var(--shiki-token-string)]">i {props.packageName}</span>
            </span>
        </CodeBlock>
      </Tab>
      <Tab>
        <CodeBlock>
            <span className="line">
              <span className="text-[var(--shiki-color-text)]">yarn </span>
              <span className="text-[var(--shiki-token-string)]">add {props.packageName}</span>
            </span>
        </CodeBlock>
      </Tab>
    </Tabs>

    Which can then be imported like this:

    <CodeBlock>
      <span className="line">
        <span className="text-[var(--shiki-token-keyword)]">import</span>
        <span className="text-[var(--shiki-color-text)]"> \{ ... } </span>
        <span className="text-[var(--shiki-token-keyword)]">from </span>
        <span className="text-[var(--shiki-token-string-expression)]">"@discokit/{props.name}"</span>
      </span>
    </CodeBlock>

  </Tab>
</Tabs>
